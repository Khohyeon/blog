<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.blog.model.BoardRepository">
   <insert id="insert">
      insert into board_tb(username, password, email, created_at) values(#{username}, #{password}, #{email}, now()) 
   </insert>
    
   <update id="updateById">
      update board_tb set username = #{username}, password=#{password}, email = #{email} where id = #{id}
   </update>

   <select id="findAll" resultType="shop.mtcoding.blog.model.Board">
      select * from board_tb
   </select>

   <select id="findOne" resultType="shop.mtcoding.blog.model.Board">
      select * from board_tb where id = #{id}  
   </select>




</mapper>